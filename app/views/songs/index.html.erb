<h1>Click a Song To View Content</h1>
<%= link_to 'Add a new song', new_board_song_path(@board) %>
<ul>
  <% @songs.each do |song| %>
    <li><%= link_to song.name, [@board, song] %></li>
  <% end %>
</ul>